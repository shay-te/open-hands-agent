x-kato-env: &kato-env
  KATO_ISSUE_PLATFORM: ${KATO_ISSUE_PLATFORM:-youtrack}
  YOUTRACK_BASE_URL: ${YOUTRACK_BASE_URL}
  YOUTRACK_TOKEN: ${YOUTRACK_TOKEN}
  YOUTRACK_PROJECT: ${YOUTRACK_PROJECT}
  YOUTRACK_ASSIGNEE: ${YOUTRACK_ASSIGNEE:-me}
  YOUTRACK_PROGRESS_STATE_FIELD: ${YOUTRACK_PROGRESS_STATE_FIELD:-State}
  YOUTRACK_PROGRESS_STATE: ${YOUTRACK_PROGRESS_STATE:-In Progress}
  YOUTRACK_ISSUE_STATES: ${YOUTRACK_ISSUE_STATES:-Todo,Open}
  YOUTRACK_REVIEW_STATE_FIELD: ${YOUTRACK_REVIEW_STATE_FIELD:-State}
  YOUTRACK_REVIEW_STATE: ${YOUTRACK_REVIEW_STATE:-To Verify}
  JIRA_BASE_URL: ${JIRA_BASE_URL:-}
  JIRA_TOKEN: ${JIRA_TOKEN:-}
  JIRA_EMAIL: ${JIRA_EMAIL:-}
  JIRA_PROJECT: ${JIRA_PROJECT:-}
  JIRA_ASSIGNEE: ${JIRA_ASSIGNEE:-}
  JIRA_PROGRESS_STATE_FIELD: ${JIRA_PROGRESS_STATE_FIELD:-status}
  JIRA_PROGRESS_STATE: ${JIRA_PROGRESS_STATE:-In Progress}
  JIRA_ISSUE_STATES: ${JIRA_ISSUE_STATES:-To Do,Open}
  JIRA_REVIEW_STATE_FIELD: ${JIRA_REVIEW_STATE_FIELD:-status}
  JIRA_REVIEW_STATE: ${JIRA_REVIEW_STATE:-In Review}
  GITHUB_ISSUES_BASE_URL: ${GITHUB_ISSUES_BASE_URL:-https://api.github.com}
  GITHUB_API_TOKEN: ${GITHUB_API_TOKEN:-}
  GITHUB_ISSUES_OWNER: ${GITHUB_ISSUES_OWNER:-}
  GITHUB_ISSUES_REPO: ${GITHUB_ISSUES_REPO:-}
  GITHUB_ISSUES_ASSIGNEE: ${GITHUB_ISSUES_ASSIGNEE:-}
  GITHUB_ISSUES_PROGRESS_STATE_FIELD: ${GITHUB_ISSUES_PROGRESS_STATE_FIELD:-labels}
  GITHUB_ISSUES_PROGRESS_STATE: ${GITHUB_ISSUES_PROGRESS_STATE:-In Progress}
  GITHUB_ISSUES_ISSUE_STATES: ${GITHUB_ISSUES_ISSUE_STATES:-open}
  GITHUB_ISSUES_REVIEW_STATE_FIELD: ${GITHUB_ISSUES_REVIEW_STATE_FIELD:-labels}
  GITHUB_ISSUES_REVIEW_STATE: ${GITHUB_ISSUES_REVIEW_STATE:-In Review}
  GITLAB_ISSUES_BASE_URL: ${GITLAB_ISSUES_BASE_URL:-https://gitlab.com/api/v4}
  GITLAB_API_TOKEN: ${GITLAB_API_TOKEN:-}
  GITLAB_ISSUES_PROJECT: ${GITLAB_ISSUES_PROJECT:-}
  GITLAB_ISSUES_ASSIGNEE: ${GITLAB_ISSUES_ASSIGNEE:-}
  GITLAB_ISSUES_PROGRESS_STATE_FIELD: ${GITLAB_ISSUES_PROGRESS_STATE_FIELD:-labels}
  GITLAB_ISSUES_PROGRESS_STATE: ${GITLAB_ISSUES_PROGRESS_STATE:-In Progress}
  GITLAB_ISSUES_ISSUE_STATES: ${GITLAB_ISSUES_ISSUE_STATES:-opened}
  GITLAB_ISSUES_REVIEW_STATE_FIELD: ${GITLAB_ISSUES_REVIEW_STATE_FIELD:-labels}
  GITLAB_ISSUES_REVIEW_STATE: ${GITLAB_ISSUES_REVIEW_STATE:-In Review}
  BITBUCKET_ISSUES_BASE_URL: ${BITBUCKET_ISSUES_BASE_URL:-https://api.bitbucket.org/2.0}
  BITBUCKET_API_TOKEN: ${BITBUCKET_API_TOKEN:-}
  BITBUCKET_USERNAME: ${BITBUCKET_USERNAME:-}
  BITBUCKET_API_EMAIL: ${BITBUCKET_API_EMAIL:-}
  BITBUCKET_ISSUES_WORKSPACE: ${BITBUCKET_ISSUES_WORKSPACE:-}
  BITBUCKET_ISSUES_REPO_SLUG: ${BITBUCKET_ISSUES_REPO_SLUG:-}
  BITBUCKET_ISSUES_ASSIGNEE: ${BITBUCKET_ISSUES_ASSIGNEE:-}
  BITBUCKET_ISSUES_PROGRESS_STATE_FIELD: ${BITBUCKET_ISSUES_PROGRESS_STATE_FIELD:-state}
  BITBUCKET_ISSUES_PROGRESS_STATE: ${BITBUCKET_ISSUES_PROGRESS_STATE:-open}
  BITBUCKET_ISSUES_ISSUE_STATES: ${BITBUCKET_ISSUES_ISSUE_STATES:-new,open}
  BITBUCKET_ISSUES_REVIEW_STATE_FIELD: ${BITBUCKET_ISSUES_REVIEW_STATE_FIELD:-state}
  BITBUCKET_ISSUES_REVIEW_STATE: ${BITBUCKET_ISSUES_REVIEW_STATE:-resolved}
  REPOSITORY_ROOT_PATH: ${REPOSITORY_ROOT_PATH:-.}
  OPENHANDS_BASE_URL: http://openhands:3000
  OPENHANDS_API_KEY: ${OPENHANDS_API_KEY:-local}
  OPENHANDS_SKIP_TESTING: ${OPENHANDS_SKIP_TESTING:-false}
  OPENHANDS_TESTING_CONTAINER_ENABLED: ${OPENHANDS_TESTING_CONTAINER_ENABLED:-false}
  OPENHANDS_TESTING_BASE_URL: http://openhands-testing:3000
  KATO_EXTERNAL_API_MAX_RETRIES: ${KATO_EXTERNAL_API_MAX_RETRIES:-5}
  KATO_FAILURE_EMAIL_ENABLED: ${KATO_FAILURE_EMAIL_ENABLED:-false}
  KATO_FAILURE_EMAIL_TEMPLATE_ID: ${KATO_FAILURE_EMAIL_TEMPLATE_ID:-0}
  KATO_FAILURE_EMAIL_TO: ${KATO_FAILURE_EMAIL_TO:-}
  KATO_FAILURE_EMAIL_SENDER_NAME: ${KATO_FAILURE_EMAIL_SENDER_NAME:-Kato}
  KATO_FAILURE_EMAIL_SENDER_EMAIL: ${KATO_FAILURE_EMAIL_SENDER_EMAIL:-noreply@example.com}
  KATO_COMPLETION_EMAIL_ENABLED: ${KATO_COMPLETION_EMAIL_ENABLED:-false}
  KATO_COMPLETION_EMAIL_TEMPLATE_ID: ${KATO_COMPLETION_EMAIL_TEMPLATE_ID:-0}
  KATO_COMPLETION_EMAIL_TO: ${KATO_COMPLETION_EMAIL_TO:-}
  KATO_COMPLETION_EMAIL_SENDER_NAME: ${KATO_COMPLETION_EMAIL_SENDER_NAME:-Kato}
  KATO_COMPLETION_EMAIL_SENDER_EMAIL: ${KATO_COMPLETION_EMAIL_SENDER_EMAIL:-noreply@example.com}
  EMAIL_CORE_LIB_SEND_IN_BLUE_API_KEY: ${EMAIL_CORE_LIB_SEND_IN_BLUE_API_KEY:-}
  SLACK_WEBHOOK_URL_ERRORS_EMAIL: ${SLACK_WEBHOOK_URL_ERRORS_EMAIL:-}
  KATO_LOG_LEVEL: ${KATO_LOG_LEVEL:-warning}
  KATO_WORKFLOW_LOG_LEVEL: ${KATO_WORKFLOW_LOG_LEVEL:-info}
  KATO_SOURCE_FINGERPRINT: ${KATO_SOURCE_FINGERPRINT:-}
  OPENHANDS_LLM_MODEL: ${OPENHANDS_LLM_MODEL:-}
  OPENHANDS_LLM_BASE_URL: ${OPENHANDS_LLM_BASE_URL:-}
  OPENHANDS_MODEL_SMOKE_TEST_ENABLED: ${OPENHANDS_MODEL_SMOKE_TEST_ENABLED:-true}
  OPENHANDS_TESTING_LLM_MODEL: ${OPENHANDS_TESTING_LLM_MODEL:-}
  OPENHANDS_TESTING_LLM_API_KEY: ${OPENHANDS_TESTING_LLM_API_KEY:-}
  OPENHANDS_TESTING_LLM_BASE_URL: ${OPENHANDS_TESTING_LLM_BASE_URL:-}
  OPENHANDS_POLL_INTERVAL_SECONDS: ${OPENHANDS_POLL_INTERVAL_SECONDS:-2.0}
  OPENHANDS_MAX_POLL_ATTEMPTS: ${OPENHANDS_MAX_POLL_ATTEMPTS:-900}
  OPENHANDS_TASK_SCAN_STARTUP_DELAY_SECONDS: ${OPENHANDS_TASK_SCAN_STARTUP_DELAY_SECONDS:-30}
  OPENHANDS_TASK_SCAN_INTERVAL_SECONDS: ${OPENHANDS_TASK_SCAN_INTERVAL_SECONDS:-60}
  LLM_MODEL: ${OPENHANDS_LLM_MODEL:-}
  LLM_API_KEY: ${OPENHANDS_LLM_API_KEY:-}
  LLM_BASE_URL: ${OPENHANDS_LLM_BASE_URL:-}
  ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
  OH_SECRET_KEY: ${OH_SECRET_KEY:-}
  AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-}
  AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-}
  AWS_REGION_NAME: ${AWS_REGION_NAME:-}
  AWS_SESSION_TOKEN: ${AWS_SESSION_TOKEN:-}
  AWS_DEFAULT_REGION: ${AWS_REGION_NAME:-}
  AWS_REGION: ${AWS_REGION_NAME:-}
  AWS_BEARER_TOKEN_BEDROCK: ${AWS_BEARER_TOKEN_BEDROCK:-}
  SSH_AUTH_SOCK: /ssh-agent
  OH_WEB_URL: ${OPENHANDS_WEB_URL:-}
  OH_PERSISTENCE_DIR: /.openhands
  AGENT_SERVER_IMAGE_REPOSITORY: ${KATO_AGENT_SERVER_IMAGE_REPOSITORY:-ghcr.io/openhands/agent-server}
  AGENT_SERVER_IMAGE_TAG: ${KATO_AGENT_SERVER_IMAGE_TAG:-1.12.0-python}

x-kato-service-env: &kato-service-env
  LOG_LEVEL: ${OPENHANDS_LOG_LEVEL:-error}
  UVICORN_LOG_LEVEL: ${OPENHANDS_LOG_LEVEL:-error}
  OH_SECRET_KEY: ${OH_SECRET_KEY:-}
  AGENT_SERVER_IMAGE_REPOSITORY: ${KATO_AGENT_SERVER_IMAGE_REPOSITORY:-ghcr.io/openhands/agent-server}
  AGENT_SERVER_IMAGE_TAG: ${KATO_AGENT_SERVER_IMAGE_TAG:-1.12.0-python}
  AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-}
  AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-}
  AWS_REGION_NAME: ${AWS_REGION_NAME:-}
  AWS_DEFAULT_REGION: ${AWS_REGION_NAME:-}
  AWS_REGION: ${AWS_REGION_NAME:-}
  AWS_BEARER_TOKEN_BEDROCK: ${AWS_BEARER_TOKEN_BEDROCK:-}
  ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
  SSH_AUTH_SOCK: /ssh-agent
  SANDBOX_VOLUMES: ${REPOSITORY_ROOT_PATH:-.}:/workspace/project:rw,${OPENHANDS_SSH_AUTH_SOCK_HOST_PATH:-/run/host-services/ssh-auth.sock}:/ssh-agent:ro
  OH_PERSISTENCE_DIR: /data

services:
  openhands:
    image: docker.openhands.dev/openhands/openhands:1.5
    pull_policy: ${OPENHANDS_PULL_POLICY:-missing}

    environment:
      <<: *kato-service-env
      LOG_ALL_EVENTS: ${OPENHANDS_CONTAINER_LOG_ALL_EVENTS}
      LLM_MODEL: ${OPENHANDS_LLM_MODEL:-}
      LLM_API_KEY: ${OPENHANDS_LLM_API_KEY:-}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      # LLM_AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-}
      # LLM_AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-}
      # LLM_AWS_REGION_NAME: ${AWS_REGION_NAME:-}
      # LLM_AWS_SESSION_TOKEN: ${AWS_SESSION_TOKEN:-}
      SANDBOX_VOLUMES: ${REPOSITORY_ROOT_PATH:-.}:/workspace/project:rw
      # Bedrock variant (kept for reference):
      # OH_AGENT_SERVER_ENV: '{"LLM_API_KEY":"${AWS_ACCESS_KEY_ID:-}/${AWS_SECRET_ACCESS_KEY:-}/${AWS_REGION_NAME:-}", "AWS_ACCESS_KEY_ID":"${AWS_ACCESS_KEY_ID:-}", "AWS_SECRET_ACCESS_KEY":"${AWS_SECRET_ACCESS_KEY:-}", "AWS_DEFAULT_REGION":"${AWS_REGION_NAME:-}", "AWS_REGION":"${AWS_REGION_NAME:-}", "LLM_AWS_ACCESS_KEY_ID":"${AWS_ACCESS_KEY_ID:-}", "LLM_AWS_SECRET_ACCESS_KEY":"${AWS_SECRET_ACCESS_KEY:-}", "LLM_AWS_REGION_NAME":"${AWS_REGION_NAME:-}", "OH_SANDBOX_VOLUMES":"${REPOSITORY_ROOT_PATH:-.}:/workspace/project:rw"}'
      OH_AGENT_SERVER_ENV: '{"LLM_MODEL":"${OPENHANDS_LLM_MODEL:-}", "LLM_API_KEY":"${OPENHANDS_LLM_API_KEY:-}", "ANTHROPIC_API_KEY":"${ANTHROPIC_API_KEY:-}", "OH_SANDBOX_VOLUMES":"${REPOSITORY_ROOT_PATH:-.}:/workspace/project:rw"}'


    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ${MOUNT_DOCKER_DATA_ROOT:-./mount_docker_data}/openhands:/data
      - ${MOUNT_DOCKER_DATA_ROOT:-./mount_docker_data}/openhands_state:/.openhands
      - ${REPOSITORY_ROOT_PATH:-.}:/workspace/project
      - ${OPENHANDS_SSH_AUTH_SOCK_HOST_PATH:-/run/host-services/ssh-auth.sock}:/ssh-agent:ro
    ports:
      - "${OPENHANDS_PORT:-3000}:3000"
    extra_hosts:
      - host.docker.internal:host-gateway

  # openhands-testing:
  #   profiles: ["testing"]
  #   image: docker.openhands.dev/openhands/openhands:1.5
  #   pull_policy: ${OPENHANDS_PULL_POLICY:-missing}
  #   environment:
  #     <<: *kato-service-env
  #     LLM_MODEL: ${OPENHANDS_TESTING_LLM_MODEL:-}
  #     LLM_API_KEY: ${OPENHANDS_TESTING_LLM_API_KEY:-}
  #     LLM_BASE_URL: ${OPENHANDS_TESTING_LLM_BASE_URL:-}
  #     OH_AGENT_SERVER_ENV: >-
  #       {"LLM_MODEL":"${OPENHANDS_TESTING_LLM_MODEL:-}","LLM_API_KEY":"${OPENHANDS_TESTING_LLM_API_KEY:-}","LLM_BASE_URL":"${OPENHANDS_TESTING_LLM_BASE_URL:-}","AWS_ACCESS_KEY_ID":"${AWS_ACCESS_KEY_ID:-}","AWS_SECRET_ACCESS_KEY":"${AWS_SECRET_ACCESS_KEY:-}","AWS_REGION_NAME":"${AWS_REGION_NAME:-}","AWS_SESSION_TOKEN":"${AWS_SESSION_TOKEN:-}","AWS_DEFAULT_REGION":"${AWS_REGION_NAME:-}","AWS_REGION":"${AWS_REGION_NAME:-}","LLM_AWS_ACCESS_KEY_ID":"${AWS_ACCESS_KEY_ID:-}","LLM_AWS_SECRET_ACCESS_KEY":"${AWS_SECRET_ACCESS_KEY:-}","LLM_AWS_REGION_NAME":"${AWS_REGION_NAME:-}","LLM_AWS_SESSION_TOKEN":"${AWS_SESSION_TOKEN:-}","AWS_BEARER_TOKEN_BEDROCK":"${AWS_BEARER_TOKEN_BEDROCK:-}","SSH_AUTH_SOCK":"/ssh-agent","OH_SANDBOX_VOLUMES":"${REPOSITORY_ROOT_PATH:-.}:/workspace/project:rw,${OPENHANDS_SSH_AUTH_SOCK_HOST_PATH:-/run/host-services/ssh-auth.sock}:/ssh-agent:ro"}
  #   volumes:
  #     - /var/run/docker.sock:/var/run/docker.sock
  #     - ${MOUNT_DOCKER_DATA_ROOT:-./mount_docker_data}/openhands-testing:/data
  #     - ${MOUNT_DOCKER_DATA_ROOT:-./mount_docker_data}/openhands_testing_state:/.openhands
  #     - ${REPOSITORY_ROOT_PATH:-.}:/workspace/project
  #     - ${OPENHANDS_SSH_AUTH_SOCK_HOST_PATH:-/run/host-services/ssh-auth.sock}:/ssh-agent:ro
  #   ports:
  #     - "${OPENHANDS_TESTING_PORT:-3001}:3000"
  #   extra_hosts:
  #     - host.docker.internal:host-gateway

  kato:
    build:
      context: .
      dockerfile: Dockerfile
    tty: true
    stdin_open: true
    entrypoint: ["/app/docker/entrypoint-run.sh"]
    depends_on:
      openhands:
        condition: service_started
    environment:
      <<: *kato-env
      REPOSITORY_ROOT_PATH: /workspace/project
    volumes:
      - ${REPOSITORY_ROOT_PATH:-.}:/workspace/project:rw
      - ${OPENHANDS_SSH_AUTH_SOCK_HOST_PATH:-/run/host-services/ssh-auth.sock}:/ssh-agent:ro
