x-openhands-agent-env: &openhands-agent-env
  OPENHANDS_AGENT_ISSUE_PLATFORM: ${OPENHANDS_AGENT_ISSUE_PLATFORM:-}
  OPENHANDS_AGENT_TICKET_SYSTEM: ${OPENHANDS_AGENT_TICKET_SYSTEM:-youtrack}
  YOUTRACK_BASE_URL: ${YOUTRACK_BASE_URL}
  YOUTRACK_TOKEN: ${YOUTRACK_TOKEN}
  YOUTRACK_PROJECT: ${YOUTRACK_PROJECT}
  YOUTRACK_ASSIGNEE: ${YOUTRACK_ASSIGNEE:-me}
  YOUTRACK_PROGRESS_STATE_FIELD: ${YOUTRACK_PROGRESS_STATE_FIELD:-State}
  YOUTRACK_PROGRESS_STATE: ${YOUTRACK_PROGRESS_STATE:-In Progress}
  YOUTRACK_ISSUE_STATES: ${YOUTRACK_ISSUE_STATES:-Todo,Open}
  YOUTRACK_REVIEW_STATE_FIELD: ${YOUTRACK_REVIEW_STATE_FIELD:-State}
  YOUTRACK_REVIEW_STATE: ${YOUTRACK_REVIEW_STATE:-In Review}
  JIRA_BASE_URL: ${JIRA_BASE_URL:-}
  JIRA_TOKEN: ${JIRA_TOKEN:-}
  JIRA_EMAIL: ${JIRA_EMAIL:-}
  JIRA_PROJECT: ${JIRA_PROJECT:-}
  JIRA_ASSIGNEE: ${JIRA_ASSIGNEE:-}
  JIRA_PROGRESS_STATE_FIELD: ${JIRA_PROGRESS_STATE_FIELD:-status}
  JIRA_PROGRESS_STATE: ${JIRA_PROGRESS_STATE:-In Progress}
  JIRA_ISSUE_STATES: ${JIRA_ISSUE_STATES:-To Do,Open}
  JIRA_REVIEW_STATE_FIELD: ${JIRA_REVIEW_STATE_FIELD:-status}
  JIRA_REVIEW_STATE: ${JIRA_REVIEW_STATE:-In Review}
  GITHUB_ISSUES_BASE_URL: ${GITHUB_ISSUES_BASE_URL:-https://api.github.com}
  GITHUB_ISSUES_TOKEN: ${GITHUB_ISSUES_TOKEN:-}
  GITHUB_ISSUES_OWNER: ${GITHUB_ISSUES_OWNER:-}
  GITHUB_ISSUES_REPO: ${GITHUB_ISSUES_REPO:-}
  GITHUB_ISSUES_ASSIGNEE: ${GITHUB_ISSUES_ASSIGNEE:-}
  GITHUB_ISSUES_PROGRESS_STATE_FIELD: ${GITHUB_ISSUES_PROGRESS_STATE_FIELD:-labels}
  GITHUB_ISSUES_PROGRESS_STATE: ${GITHUB_ISSUES_PROGRESS_STATE:-In Progress}
  GITHUB_ISSUES_ISSUE_STATES: ${GITHUB_ISSUES_ISSUE_STATES:-open}
  GITHUB_ISSUES_REVIEW_STATE_FIELD: ${GITHUB_ISSUES_REVIEW_STATE_FIELD:-labels}
  GITHUB_ISSUES_REVIEW_STATE: ${GITHUB_ISSUES_REVIEW_STATE:-In Review}
  GITLAB_ISSUES_BASE_URL: ${GITLAB_ISSUES_BASE_URL:-https://gitlab.com/api/v4}
  GITLAB_ISSUES_TOKEN: ${GITLAB_ISSUES_TOKEN:-}
  GITLAB_ISSUES_PROJECT: ${GITLAB_ISSUES_PROJECT:-}
  GITLAB_ISSUES_ASSIGNEE: ${GITLAB_ISSUES_ASSIGNEE:-}
  GITLAB_ISSUES_PROGRESS_STATE_FIELD: ${GITLAB_ISSUES_PROGRESS_STATE_FIELD:-labels}
  GITLAB_ISSUES_PROGRESS_STATE: ${GITLAB_ISSUES_PROGRESS_STATE:-In Progress}
  GITLAB_ISSUES_ISSUE_STATES: ${GITLAB_ISSUES_ISSUE_STATES:-opened}
  GITLAB_ISSUES_REVIEW_STATE_FIELD: ${GITLAB_ISSUES_REVIEW_STATE_FIELD:-labels}
  GITLAB_ISSUES_REVIEW_STATE: ${GITLAB_ISSUES_REVIEW_STATE:-In Review}
  BITBUCKET_ISSUES_BASE_URL: ${BITBUCKET_ISSUES_BASE_URL:-https://api.bitbucket.org/2.0}
  BITBUCKET_ISSUES_TOKEN: ${BITBUCKET_ISSUES_TOKEN:-}
  BITBUCKET_ISSUES_WORKSPACE: ${BITBUCKET_ISSUES_WORKSPACE:-}
  BITBUCKET_ISSUES_REPO_SLUG: ${BITBUCKET_ISSUES_REPO_SLUG:-}
  BITBUCKET_ISSUES_ASSIGNEE: ${BITBUCKET_ISSUES_ASSIGNEE:-}
  BITBUCKET_ISSUES_PROGRESS_STATE_FIELD: ${BITBUCKET_ISSUES_PROGRESS_STATE_FIELD:-state}
  BITBUCKET_ISSUES_PROGRESS_STATE: ${BITBUCKET_ISSUES_PROGRESS_STATE:-open}
  BITBUCKET_ISSUES_ISSUE_STATES: ${BITBUCKET_ISSUES_ISSUE_STATES:-new,open}
  BITBUCKET_ISSUES_REVIEW_STATE_FIELD: ${BITBUCKET_ISSUES_REVIEW_STATE_FIELD:-state}
  BITBUCKET_ISSUES_REVIEW_STATE: ${BITBUCKET_ISSUES_REVIEW_STATE:-resolved}
  REPOSITORY_ROOT_PATH: ${REPOSITORY_ROOT_PATH:-.}
  OPENHANDS_AGENT_DB_PROTOCOL: ${OPENHANDS_AGENT_DB_PROTOCOL:-sqlite}
  OPENHANDS_AGENT_DB_USERNAME: ${OPENHANDS_AGENT_DB_USERNAME:-}
  OPENHANDS_AGENT_DB_PASSWORD: ${OPENHANDS_AGENT_DB_PASSWORD:-}
  OPENHANDS_AGENT_DB_HOST: ${OPENHANDS_AGENT_DB_HOST:-}
  OPENHANDS_AGENT_DB_PORT: ${OPENHANDS_AGENT_DB_PORT:-}
  OPENHANDS_AGENT_DB_PATH: ${OPENHANDS_AGENT_DB_PATH:-data}
  OPENHANDS_AGENT_DB_FILE: ${OPENHANDS_AGENT_DB_FILE:-openhands_agent.db}
  OPENHANDS_BASE_URL: http://openhands:3000
  OPENHANDS_API_KEY: ${OPENHANDS_API_KEY:-local}
  OPENHANDS_AGENT_MAX_RETRIES: ${OPENHANDS_AGENT_MAX_RETRIES:-5}
  OPENHANDS_AGENT_STATE_FILE: ${OPENHANDS_AGENT_STATE_FILE:-data/openhands_agent_state.json}
  OPENHANDS_AGENT_FAILURE_EMAIL_ENABLED: ${OPENHANDS_AGENT_FAILURE_EMAIL_ENABLED:-false}
  OPENHANDS_AGENT_FAILURE_EMAIL_TEMPLATE_ID: ${OPENHANDS_AGENT_FAILURE_EMAIL_TEMPLATE_ID:-0}
  OPENHANDS_AGENT_FAILURE_EMAIL_TO: ${OPENHANDS_AGENT_FAILURE_EMAIL_TO:-}
  OPENHANDS_AGENT_FAILURE_EMAIL_SENDER_NAME: ${OPENHANDS_AGENT_FAILURE_EMAIL_SENDER_NAME:-OpenHands Agent}
  OPENHANDS_AGENT_FAILURE_EMAIL_SENDER_EMAIL: ${OPENHANDS_AGENT_FAILURE_EMAIL_SENDER_EMAIL:-noreply@example.com}
  OPENHANDS_AGENT_COMPLETION_EMAIL_ENABLED: ${OPENHANDS_AGENT_COMPLETION_EMAIL_ENABLED:-false}
  OPENHANDS_AGENT_COMPLETION_EMAIL_TEMPLATE_ID: ${OPENHANDS_AGENT_COMPLETION_EMAIL_TEMPLATE_ID:-0}
  OPENHANDS_AGENT_COMPLETION_EMAIL_TO: ${OPENHANDS_AGENT_COMPLETION_EMAIL_TO:-}
  OPENHANDS_AGENT_COMPLETION_EMAIL_SENDER_NAME: ${OPENHANDS_AGENT_COMPLETION_EMAIL_SENDER_NAME:-OpenHands Agent}
  OPENHANDS_AGENT_COMPLETION_EMAIL_SENDER_EMAIL: ${OPENHANDS_AGENT_COMPLETION_EMAIL_SENDER_EMAIL:-noreply@example.com}
  EMAIL_CORE_LIB_SEND_IN_BLUE_API_KEY: ${EMAIL_CORE_LIB_SEND_IN_BLUE_API_KEY:-}
  SLACK_WEBHOOK_URL_ERRORS_EMAIL: ${SLACK_WEBHOOK_URL_ERRORS_EMAIL:-}
  OPENHANDS_LLM_MODEL: ${OPENHANDS_LLM_MODEL:-}
  OPENHANDS_LLM_BASE_URL: ${OPENHANDS_LLM_BASE_URL:-}
  LLM_MODEL: ${OPENHANDS_LLM_MODEL:-}
  LLM_API_KEY: ${OPENHANDS_LLM_API_KEY:-}
  LLM_BASE_URL: ${OPENHANDS_LLM_BASE_URL:-}
  AWS_BEARER_TOKEN_BEDROCK: ${AWS_BEARER_TOKEN_BEDROCK:-}
  OH_WEB_URL: ${OPENHANDS_WEB_URL:-}
  OH_PERSISTENCE_DIR: /.openhands
  AGENT_SERVER_IMAGE_REPOSITORY: ${OPENHANDS_AGENT_SERVER_IMAGE_REPOSITORY:-ghcr.io/openhands/agent-server}
  AGENT_SERVER_IMAGE_TAG: ${OPENHANDS_AGENT_SERVER_IMAGE_TAG:-1.12.0-python}

services:
  openhands:
    image: docker.openhands.dev/openhands/openhands:1.5
    pull_policy: always
    environment:
      LLM_MODEL: ${OPENHANDS_LLM_MODEL:-}
      LLM_API_KEY: ${OPENHANDS_LLM_API_KEY:-}
      LLM_BASE_URL: ${OPENHANDS_LLM_BASE_URL:-}
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-}
      AWS_REGION_NAME: ${AWS_REGION_NAME:-}
      AWS_DEFAULT_REGION: ${AWS_REGION_NAME:-}
      AWS_REGION: ${AWS_REGION_NAME:-}
      AWS_BEARER_TOKEN_BEDROCK: ${AWS_BEARER_TOKEN_BEDROCK:-}
      SANDBOX_VOLUMES: ${REPOSITORY_ROOT_PATH:-.}:/workspace/project:rw
      OH_AGENT_SERVER_ENV: >-
        {"LLM_MODEL":"${OPENHANDS_LLM_MODEL:-}","LLM_API_KEY":"${OPENHANDS_LLM_API_KEY:-}","LLM_BASE_URL":"${OPENHANDS_LLM_BASE_URL:-}","AWS_ACCESS_KEY_ID":"${AWS_ACCESS_KEY_ID:-}","AWS_SECRET_ACCESS_KEY":"${AWS_SECRET_ACCESS_KEY:-}","AWS_REGION_NAME":"${AWS_REGION_NAME:-}","AWS_DEFAULT_REGION":"${AWS_REGION_NAME:-}","AWS_REGION":"${AWS_REGION_NAME:-}","LLM_AWS_ACCESS_KEY_ID":"${AWS_ACCESS_KEY_ID:-}","LLM_AWS_SECRET_ACCESS_KEY":"${AWS_SECRET_ACCESS_KEY:-}","LLM_AWS_REGION_NAME":"${AWS_REGION_NAME:-}","AWS_BEARER_TOKEN_BEDROCK":"${AWS_BEARER_TOKEN_BEDROCK:-}","OH_SANDBOX_VOLUMES":"${REPOSITORY_ROOT_PATH:-.}:/workspace/project:rw"}
      OH_PERSISTENCE_DIR: /data
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./docker_data/openhands:/data
      - ./docker_data/openhands_state:/.openhands
      - ${REPOSITORY_ROOT_PATH:-.}:/workspace/project
    ports:
      - "${OPENHANDS_PORT:-3000}:3000"
    extra_hosts:
      - host.docker.internal:host-gateway

  install:
    build:
      context: .
      dockerfile: Dockerfile
    entrypoint: ["/app/docker/entrypoint-install.sh"]
    environment: *openhands-agent-env
    volumes:
      - openhands-agent-data:/app/data
    restart: "no"

  openhands-agent:
    build:
      context: .
      dockerfile: Dockerfile
    entrypoint: ["/app/docker/entrypoint-run.sh"]
    depends_on:
      openhands:
        condition: service_started
      install:
        condition: service_completed_successfully
    environment:
      <<: *openhands-agent-env
      REPOSITORY_ROOT_PATH: /workspace/project
    volumes:
      - openhands-agent-data:/app/data
      - ${REPOSITORY_ROOT_PATH:-.}:/workspace/project:ro

volumes:
  openhands-agent-data:
